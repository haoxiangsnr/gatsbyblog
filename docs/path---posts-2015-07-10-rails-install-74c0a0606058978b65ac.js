webpackJsonp([0xd9c50bb4332b],{433:function(n,s){n.exports={data:{markdownRemark:{html:'<h1>Rails 安装记录</h1>\n<blockquote>\n<p>本文<code>ubuntu</code>版本为<code>14.04stl 64bit</code>，参考教程：<a href="http://rails-practice.com/content/Chapter_1/1.1.html%EF%BC%8C">http://rails-practice.com/content/Chapter_1/1.1.html，</a> 需要翻墙</p>\n</blockquote>\n<h2>Git</h2>\n<p>ubuntu安装完毕之后，首先安装git，以便之后会用到：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt-get</span> update\n<span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> git-core\n</code></pre>\n      </div>\n<p>配置身份信息：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">git</span> config --global user.name <span class="token string">"xxx"</span>\n<span class="token function">git</span> config --global user.email <span class="token string">"xx@gmail.com"</span>\n</code></pre>\n      </div>\n<p>核实信息是否有误：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">git</span> config --list\n</code></pre>\n      </div>\n<p>此时已经能通过<code>http</code>正常拉去git上的内容，若想进一步配置<code>ssh</code>，可以参考<a href="https://help.github.com/articles/generating-ssh-keys/1323">https://help.github.com/articles/generating-ssh-keys/1323</a></p>\n<h2>RVM</h2>\n<p><code>rails</code>需要先安装<code>Ruby</code>环境，<code>RVM</code>是<code>Ruby</code>管理工具，可以方便的安装、管理、切换多个<code>Ruby</code>，管理<code>Gemset</code>。</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>curl -sSL https://get.rvm.io <span class="token operator">|</span> <span class="token function">bash</span> -s stable\n</code></pre>\n      </div>\n<p>此时会有如下提示：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>getcha22@ubuntu:~/Desktop$ curl -sSL https://get.rvm.io <span class="token operator">|</span> <span class="token function">bash</span> -s stable\nDownloading https://github.com/rvm/rvm/archive/1.26.11.tar.gz\nDownloading https://github.com/rvm/rvm/releases/download/1.26.11/1.26.11.tar.gz.asc\ngpg: Signature made Mon 30 Mar 2015 02:52:13 PM PDT using RSA key ID BF04FF17\ngpg: Can<span class="token string">\'t check signature: public key not found\nWarning, RVM 1.26.0 introduces signed releases and automated check of signatures when GPG software found.\nAssuming you trust Michal Papis import the mpapis public key (downloading the signatures).\nGPG signature verification failed for \'</span>/home/getcha22/.rvm/archives/rvm-1.26.11.tgz<span class="token string">\' - \'</span>https://github.com/rvm/rvm/releases/download/1.26.11/1.26.11.tar.gz.asc\'<span class="token operator">!</span>\ntry downloading the signatures:\n    gpg --keyserver hkp://keys.gnupg.net --recv-keys 409B6B1796C275462A1703113804BB82D39DC0E3\nor <span class="token keyword">if</span> it fails:\n    <span class="token function">command</span> curl -sSL https://rvm.io/mpapis.asc <span class="token operator">|</span> gpg --import -\nthe key can be compared with:\n    https://rvm.io/mpapis.asc\n    https://keybase.io/mpapi\n</code></pre>\n      </div>\n<p>按照提示，执行：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>gpg --keyserver hkp://keys.gnupg.net --recv-keys 409B6B1796C275462A1703113804BB82D39DC0\n</code></pre>\n      </div>\n<p>依然warring，继续按照提示</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token function">sudo</span> curl -sSL https://rvm.io/mpapis.asc <span class="token operator">|</span> gpg --import -\n</code></pre>\n      </div>\n<p>提示Thank you for using RVM!，OK，安装成功</p>\n<h2>Ruby</h2>\n<p>重启终端，开始安装<code>Ruby</code>，先使用<code>rvm list known</code>来查看可以按照的<code>Ruby</code>列表，按照参考的教程，我选择使用<code>2.2.0</code></p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>rvm <span class="token function">install</span> 2.2.0123\n</code></pre>\n      </div>\n<p>在我安装的过程中，由于vpn不算稳定，出现了404的提示，我尝试重新安装，这次安装成功。 通过下面的命令检查当前的版本：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>rvm list\n</code></pre>\n      </div>\n<p>提示结果：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>rvm rubies\n   ruby-2.2.0 <span class="token punctuation">[</span> x86_64 <span class="token punctuation">]</span>\n</code></pre>\n      </div>\n<p>再进一步核实<code>ruby</code>的版本， 此时由于<code>ubuntu</code>存在其他版本的<code>ruby</code>，需要制定默认的<code>ruby</code>版本，此时可以使用:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>rvm --default use 2.2.0\n</code></pre>\n      </div>\n<p>发现有错误提示<code>RVM is not a function</code>,这个问题是由于<code>ubuntu</code>系统的终端导致 ，为了解决这个问题，需要<code>Run command as login shell</code>，参考这个链接进行解决：<a href="https://rvm.io/integration/gnome-terminal">https://rvm.io/integration/gnome-terminal</a> ，之后重新制定默认版本，提示OK。</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>ruby 2.2.0p0 <span class="token punctuation">(</span>2014-12-25 revision 49005<span class="token punctuation">)</span> <span class="token punctuation">[</span>x86_64-linux<span class="token punctuation">]</span>\n</code></pre>\n      </div>\n<p>到此为止，<code>ruby</code>的安装告一段落，开始进行<code>Rails</code>的安装。</p>\n<h2>Rails</h2>\n<p>先设置<code>Gemset</code>。<code>Gemset</code> 是一个独立的 <code>Gem</code> 集合，可以为每个项目设置自己的 <code>Gemset</code>，而不会相互干扰：</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>rvm gemset create rails4.2\nrvm use 2.2.0@rails4.2 --default\ngem <span class="token function">install</span> rails -v 4.2.0 --no-ri --no-rdoc\n</code></pre>\n      </div>\n<p>出现如下问题:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>ERROR:  While executing gem <span class="token punctuation">..</span>. <span class="token punctuation">(</span>Gem::RemoteFetcher::FetchError<span class="token punctuation">)</span>\n    Errno::ECONNRESET: Connection reset by peer - connect<span class="token punctuation">(</span>2<span class="token punctuation">)</span> <span class="token keyword">for</span> <span class="token string">"rubygems.global.ssl.fastly.net"</span> port 443 <span class="token punctuation">(</span>https://api.rubygems.org/quick/Marshal.4.8/rails-4.2.4.gemspec.rz<span class="token punctuation">)</span>\n</code></pre>\n      </div>\n<p>遇到这个问题，还提示端口是443.可能是<code>gem</code>源导致的网络问题，尝试修改https为http。通过以下命令:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>gem sources\ngem sources -a url //添加\ngem sources -r url  //删除\n更新source cache\ngem sources -u\n// other <span class="token keyword">:</span>\ngem sources --add https://ruby.taobao.org/ --remove https://rubygems.org/\ngem sources -l\n</code></pre>\n      </div>\n<p>之后重新进行安装，最后结束安装</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>Successfully installed rails-4.2.0\n30 gems installed\n</code></pre>\n      </div>\n<h2>其他情况</h2>\n<ul>\n<li><code>bundle install</code>如果遇到问题，可以先把<code>gemfile</code>中的<code>source</code>修改成<code>http</code>开头即可，如果在操作时提示需要<code>javascript run time</code>，只要安装<code>Node.js</code>即可</li>\n</ul>',frontmatter:{title:"Rails 安装记录",date:"2015-07-10",tags:["Ruby","Rails","installtion-record"]}}},pathContext:{slug:"/2015-07-10---rails-install/"}}}});
//# sourceMappingURL=path---posts-2015-07-10-rails-install-74c0a0606058978b65ac.js.map